distribute result [$$38]
-- DISTRIBUTE_RESULT  |PARTITIONED|
  exchange
  -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
    assign [$$38] <- [{"business_id": $$39}] project: [$$38]
    -- ASSIGN  |PARTITIONED|
      select (and(eq("19:49:16", $$D.getField(1)), eq("2016-04-26", $$D.getField(0)))) project: [$$39]
      -- STREAM_SELECT  |PARTITIONED|
        unnest $$D <- scan-collection($$41) project: [$$39, $$D]
        -- UNNEST  |PARTITIONED|
          select (eq("--1UhMGODdWsrMastO9DZw", $$39))
          -- STREAM_SELECT  |PARTITIONED|
            assign [$$39, $$41] <- [$$C.getField(1), $$C.getField(2)] project: [$$39, $$41]
            -- ASSIGN  |PARTITIONED|
              project ([$$C])
              -- STREAM_PROJECT  |PARTITIONED|
                exchange
                -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                  data-scan []<-[$$40, $$C] <- TestYelp.YelpCheckin
                  -- DATASOURCE_SCAN  |PARTITIONED|
                    exchange
                    -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                      empty-tuple-source
                      -- EMPTY_TUPLE_SOURCE  |PARTITIONED|
