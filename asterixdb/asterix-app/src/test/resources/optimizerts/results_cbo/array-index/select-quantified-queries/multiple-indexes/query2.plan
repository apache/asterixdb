distribute result [$$38]
-- DISTRIBUTE_RESULT  |PARTITIONED|
  exchange
  -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
    assign [$$38] <- [{"k": $$k}] project: [$$38]
    -- ASSIGN  |PARTITIONED|
      select ($$36) project: [$$k]
      -- STREAM_SELECT  |PARTITIONED|
        project ([$$k, $$36])
        -- STREAM_PROJECT  |PARTITIONED|
          subplan {
                    aggregate [$$36] <- [non-empty-stream()]
                    -- AGGREGATE  |LOCAL|
                      select (and(eq($$43, 263), eq($$42, 123), eq($$41, 284)))
                      -- STREAM_SELECT  |LOCAL|
                        assign [$$43, $$42, $$41] <- [$$v.getField("b"), $$v.getField("c"), $$v.getField("a")] project: [$$43, $$42, $$41]
                        -- ASSIGN  |LOCAL|
                          unnest $$v <- scan-collection($$40) project: [$$v]
                          -- UNNEST  |LOCAL|
                            project ([$$40])
                            -- STREAM_PROJECT  |LOCAL|
                              nested tuple source
                              -- NESTED_TUPLE_SOURCE  |LOCAL|
                 }
          -- SUBPLAN  |PARTITIONED|
            assign [$$40] <- [$$k.getField("uarr_i")]
            -- ASSIGN  |PARTITIONED|
              project ([$$k])
              -- STREAM_PROJECT  |PARTITIONED|
                exchange
                -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                  data-scan []<-[$$39, $$k] <- test.KSI
                  -- DATASOURCE_SCAN  |PARTITIONED|
                    exchange
                    -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                      empty-tuple-source
                      -- EMPTY_TUPLE_SOURCE  |PARTITIONED|
