distribute result [$$42]
-- DISTRIBUTE_RESULT  |PARTITIONED|
  exchange
  -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
    assign [$$42] <- [{"c0": $$51, "c1": $$52}] project: [$$42]
    -- ASSIGN  |PARTITIONED|
      project ([$$51, $$52])
      -- STREAM_PROJECT  |PARTITIONED|
        exchange
        -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
          join (and(eq($$45, $$46), eq($$47, $$48), eq($$49, $$50)))
          -- HYBRID_HASH_JOIN [$$45, $$47, $$49][$$46, $$48, $$50]  |PARTITIONED|
            exchange
            -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
              assign [$$51, $$49, $$47, $$45] <- [$$t1.getField(4), $$t1.getField(1), $$t1.getField(2), $$t1.getField(3)] project: [$$51, $$45, $$47, $$49]
              -- ASSIGN  |PARTITIONED|
                project ([$$t1])
                -- STREAM_PROJECT  |PARTITIONED|
                  exchange
                  -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                    data-scan []<-[$$43, $$t1] <- test.t1
                    -- DATASOURCE_SCAN  |PARTITIONED|
                      exchange
                      -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                        empty-tuple-source
                        -- EMPTY_TUPLE_SOURCE  |PARTITIONED|
            exchange
            -- BROADCAST_EXCHANGE  |PARTITIONED|
              assign [$$52, $$50, $$48, $$46] <- [$$t2.getField(4), $$t2.getField(1), $$t2.getField(2), $$t2.getField(3)] project: [$$52, $$46, $$48, $$50]
              -- ASSIGN  |PARTITIONED|
                project ([$$t2])
                -- STREAM_PROJECT  |PARTITIONED|
                  exchange
                  -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                    data-scan []<-[$$44, $$t2] <- test.t2
                    -- DATASOURCE_SCAN  |PARTITIONED|
                      exchange
                      -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                        empty-tuple-source
                        -- EMPTY_TUPLE_SOURCE  |PARTITIONED|
