distribute result [$$222]
-- DISTRIBUTE_RESULT  |PARTITIONED|
  exchange
  -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
    assign [$$222] <- [{"o_id": $$o_id, "o_w_id": $$o_w_id, "o_d_id": $$o_d_id, "revenue": $$244, "o_entry_d": $$o_entry_d}] project: [$$222]
    -- ASSIGN  |PARTITIONED|
      exchange
      -- SORT_MERGE_EXCHANGE [$$244(DESC), $$o_entry_d(ASC) ]  |PARTITIONED|
        order (DESC, $$244) (ASC, $$o_entry_d)
        -- STABLE_SORT [$$244(DESC), $$o_entry_d(ASC)]  |PARTITIONED|
          exchange
          -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
            group by ([$$o_id := $$250; $$o_w_id := $$251; $$o_d_id := $$252; $$o_entry_d := $$253]) decor ([]) {
                      aggregate [$$244] <- [agg-global-sql-sum($$249)]
                      -- AGGREGATE  |LOCAL|
                        nested tuple source
                        -- NESTED_TUPLE_SOURCE  |LOCAL|
                   }
            -- SORT_GROUP_BY[$$250, $$251, $$252, $$253]  |PARTITIONED|
              exchange
              -- HASH_PARTITION_EXCHANGE [$$250, $$251, $$252, $$253]  |PARTITIONED|
                group by ([$$250 := $$245; $$251 := $$228; $$252 := $$229; $$253 := $$230]) decor ([]) {
                          aggregate [$$249] <- [agg-local-sql-sum($$219)]
                          -- AGGREGATE  |LOCAL|
                            nested tuple source
                            -- NESTED_TUPLE_SOURCE  |LOCAL|
                       }
                -- SORT_GROUP_BY[$$245, $$228, $$229, $$230]  |PARTITIONED|
                  exchange
                  -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                    project ([$$219, $$245, $$228, $$229, $$230])
                    -- STREAM_PROJECT  |PARTITIONED|
                      exchange
                      -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                        join (and(eq($$241, $$228), eq($$242, $$229), eq($$243, $$245)))
                        -- HYBRID_HASH_JOIN [$$228, $$229, $$245][$$241, $$242, $$243]  |PARTITIONED|
                          exchange
                          -- HASH_PARTITION_EXCHANGE [$$228, $$229, $$245]  |PARTITIONED|
                            assign [$$219] <- [$$ol.getField("ol_amount")] project: [$$219, $$245, $$228, $$229, $$230]
                            -- ASSIGN  |PARTITIONED|
                              unnest $$ol <- scan-collection($$246) project: [$$245, $$230, $$229, $$228, $$ol]
                              -- UNNEST  |PARTITIONED|
                                project ([$$246, $$245, $$230, $$229, $$228])
                                -- STREAM_PROJECT  |PARTITIONED|
                                  exchange
                                  -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                                    join (and(eq($$237, $$229), eq($$238, $$228), eq($$239, $$240)))
                                    -- HYBRID_HASH_JOIN [$$229, $$228, $$240][$$237, $$238, $$239]  |PARTITIONED|
                                      exchange
                                      -- HASH_PARTITION_EXCHANGE [$$229, $$228, $$240]  |PARTITIONED|
                                        select (lt($$230, "2017-03-15 00:00:00.000000"))
                                        -- STREAM_SELECT  |PARTITIONED|
                                          assign [$$246, $$245, $$240, $$230, $$229, $$228] <- [$$o.getField("o_orderline"), $$o.getField("o_id"), $$o.getField("o_c_id"), $$o.getField("o_entry_d"), $$o.getField("o_d_id"), $$o.getField("o_w_id")] project: [$$246, $$245, $$240, $$230, $$229, $$228]
                                          -- ASSIGN  |PARTITIONED|
                                            project ([$$o])
                                            -- STREAM_PROJECT  |PARTITIONED|
                                              exchange
                                              -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                                                data-scan []<-[$$234, $$o] <- test.orders
                                                -- DATASOURCE_SCAN  |PARTITIONED|
                                                  exchange
                                                  -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                                                    empty-tuple-source
                                                    -- EMPTY_TUPLE_SOURCE  |PARTITIONED|
                                      exchange
                                      -- HASH_PARTITION_EXCHANGE [$$237, $$238, $$239]  |PARTITIONED|
                                        select (and(lt($$227, "B"), ge($$227, "A"))) project: [$$237, $$238, $$239]
                                        -- STREAM_SELECT  |PARTITIONED|
                                          assign [$$239, $$238, $$237, $$227] <- [$$c.getField("c_id"), $$c.getField("c_w_id"), $$c.getField("c_d_id"), $$c.getField("c_state")] project: [$$239, $$238, $$237, $$227]
                                          -- ASSIGN  |PARTITIONED|
                                            project ([$$c])
                                            -- STREAM_PROJECT  |PARTITIONED|
                                              exchange
                                              -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                                                data-scan []<-[$$235, $$c] <- test.customer
                                                -- DATASOURCE_SCAN  |PARTITIONED|
                                                  exchange
                                                  -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                                                    empty-tuple-source
                                                    -- EMPTY_TUPLE_SOURCE  |PARTITIONED|
                          exchange
                          -- HASH_PARTITION_EXCHANGE [$$241, $$242, $$243]  |PARTITIONED|
                            assign [$$243, $$242, $$241] <- [$$no.getField("no_o_id"), $$no.getField("no_d_id"), $$no.getField("no_w_id")] project: [$$241, $$242, $$243]
                            -- ASSIGN  |PARTITIONED|
                              project ([$$no])
                              -- STREAM_PROJECT  |PARTITIONED|
                                exchange
                                -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                                  data-scan []<-[$$236, $$no] <- test.neworder
                                  -- DATASOURCE_SCAN  |PARTITIONED|
                                    exchange
                                    -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                                      empty-tuple-source
                                      -- EMPTY_TUPLE_SOURCE  |PARTITIONED|
