distribute result [$$108]
-- DISTRIBUTE_RESULT  |PARTITIONED|
  exchange
  -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
    assign [$$108] <- [{"cid": $$119, "pid": $$115, "ts": $$111}] project: [$$108]
    -- ASSIGN  |PARTITIONED|
      exchange
      -- SORT_MERGE_EXCHANGE [$$119(ASC) ]  |PARTITIONED|
        order (ASC, $$119)
        -- STABLE_SORT [$$119(ASC)]  |PARTITIONED|
          exchange
          -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
            select ($$102) project: [$$119, $$115, $$111]
            -- STREAM_SELECT  |PARTITIONED|
              project ([$$102, $$115, $$119, $$111])
              -- STREAM_PROJECT  |PARTITIONED|
                exchange
                -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                  group by ([$$124 := $$122]) decor ([$$115; $$119; $$111]) {
                            aggregate [$$102] <- [non-empty-stream()]
                            -- AGGREGATE  |LOCAL|
                              select (not(is-missing($$123)))
                              -- STREAM_SELECT  |LOCAL|
                                project ([$$123])
                                -- STREAM_PROJECT  |LOCAL|
                                  nested tuple source
                                  -- NESTED_TUPLE_SOURCE  |LOCAL|
                         }
                  -- PRE_CLUSTERED_GROUP_BY[$$122]  |PARTITIONED|
                    exchange
                    -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                      order (ASC, $$122)
                      -- STABLE_SORT [$$122(ASC)]  |PARTITIONED|
                        exchange
                        -- HASH_PARTITION_EXCHANGE [$$122]  |PARTITIONED|
                          project ([$$119, $$115, $$111, $$123, $$122])
                          -- STREAM_PROJECT  |PARTITIONED|
                            exchange
                            -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                              left outer join (eq($$115, $$pid))
                              -- HYBRID_HASH_JOIN [$$115][$$pid]  |PARTITIONED|
                                exchange
                                -- HASH_PARTITION_EXCHANGE [$$115]  |PARTITIONED|
                                  select (ge($$111, 2000))
                                  -- STREAM_SELECT  |PARTITIONED|
                                    running-aggregate [$$122] <- [create-query-uid()]
                                    -- RUNNING_AGGREGATE  |PARTITIONED|
                                      assign [$$111, $$115] <- [$$i1.getField("ts"), $$i1.getField("pid")] project: [$$119, $$115, $$111]
                                      -- ASSIGN  |PARTITIONED|
                                        unnest $$i1 <- scan-collection($$116) project: [$$119, $$i1]
                                        -- UNNEST  |PARTITIONED|
                                          assign [$$119, $$116] <- [$$c1.getField("cid"), $$c1.getField("items")] project: [$$119, $$116]
                                          -- ASSIGN  |PARTITIONED|
                                            assign [$$c1] <- [$$c2] project: [$$c1]
                                            -- ASSIGN  |PARTITIONED|
                                              exchange
                                              -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                                                replicate
                                                -- REPLICATE  |PARTITIONED|
                                                  exchange
                                                  -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                                                    project ([$$c2])
                                                    -- STREAM_PROJECT  |PARTITIONED|
                                                      exchange
                                                      -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                                                        data-scan []<-[$$113, $$c2] <- test.cart
                                                        -- DATASOURCE_SCAN  |PARTITIONED|
                                                          exchange
                                                          -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                                                            empty-tuple-source
                                                            -- EMPTY_TUPLE_SOURCE  |PARTITIONED|
                                exchange
                                -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                                  assign [$$123] <- [true]
                                  -- ASSIGN  |PARTITIONED|
                                    select (gt($$114, 1)) project: [$$pid]
                                    -- STREAM_SELECT  |PARTITIONED|
                                      exchange
                                      -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                                        group by ([$$pid := $$126]) decor ([]) {
                                                  aggregate [$$114] <- [agg-sql-sum($$125)]
                                                  -- AGGREGATE  |LOCAL|
                                                    nested tuple source
                                                    -- NESTED_TUPLE_SOURCE  |LOCAL|
                                               }
                                        -- SORT_GROUP_BY[$$126]  |PARTITIONED|
                                          exchange
                                          -- HASH_PARTITION_EXCHANGE [$$126]  |PARTITIONED|
                                            group by ([$$126 := $$110]) decor ([]) {
                                                      aggregate [$$125] <- [agg-sql-count(1)]
                                                      -- AGGREGATE  |LOCAL|
                                                        nested tuple source
                                                        -- NESTED_TUPLE_SOURCE  |LOCAL|
                                                   }
                                            -- SORT_GROUP_BY[$$110]  |PARTITIONED|
                                              exchange
                                              -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                                                select (ge($$i2.getField("ts"), 2000)) project: [$$110]
                                                -- STREAM_SELECT  |PARTITIONED|
                                                  assign [$$110] <- [$$i2.getField("pid")]
                                                  -- ASSIGN  |PARTITIONED|
                                                    unnest $$i2 <- scan-collection($$117) project: [$$i2]
                                                    -- UNNEST  |PARTITIONED|
                                                      assign [$$117] <- [$$c2.getField("items")] project: [$$117]
                                                      -- ASSIGN  |PARTITIONED|
                                                        exchange
                                                        -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                                                          replicate
                                                          -- REPLICATE  |PARTITIONED|
                                                            exchange
                                                            -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                                                              project ([$$c2])
                                                              -- STREAM_PROJECT  |PARTITIONED|
                                                                exchange
                                                                -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                                                                  data-scan []<-[$$113, $$c2] <- test.cart
                                                                  -- DATASOURCE_SCAN  |PARTITIONED|
                                                                    exchange
                                                                    -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                                                                      empty-tuple-source
                                                                      -- EMPTY_TUPLE_SOURCE  |PARTITIONED|
