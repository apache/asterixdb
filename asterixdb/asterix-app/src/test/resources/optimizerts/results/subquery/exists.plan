distribute result [$$179]
-- DISTRIBUTE_RESULT  |PARTITIONED|
  exchange
  -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
    assign [$$179] <- [{"cntrycode": $$cntrycode, "numcust": $$182, "totacctbal": $$183}] project: [$$179]
    -- ASSIGN  |PARTITIONED|
      exchange
      -- SORT_MERGE_EXCHANGE [$$cntrycode(ASC) ]  |PARTITIONED|
        group by ([$$cntrycode := $$205]) decor ([]) {
                  aggregate [$$182, $$183] <- [agg-sql-sum($$203), agg-global-sql-sum($$204)]
                  -- AGGREGATE  |LOCAL|
                    nested tuple source
                    -- NESTED_TUPLE_SOURCE  |LOCAL|
               }
        -- SORT_GROUP_BY[$$205]  |PARTITIONED|
          exchange
          -- HASH_PARTITION_EXCHANGE [$$205]  |PARTITIONED|
            group by ([$$205 := $$180]) decor ([]) {
                      aggregate [$$203, $$204] <- [agg-sql-count($$140), agg-local-sql-sum($$191)]
                      -- AGGREGATE  |LOCAL|
                        nested tuple source
                        -- NESTED_TUPLE_SOURCE  |LOCAL|
                   }
            -- SORT_GROUP_BY[$$180]  |PARTITIONED|
              exchange
              -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                assign [$$180] <- [substring($$193, 0, 2)] project: [$$140, $$191, $$180]
                -- ASSIGN  |PARTITIONED|
                  select (neq($$181, 0)) project: [$$193, $$140, $$191]
                  -- STREAM_SELECT  |PARTITIONED|
                    project ([$$181, $$193, $$140, $$191])
                    -- STREAM_PROJECT  |PARTITIONED|
                      exchange
                      -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                        group by ([$$198 := $$202]) decor ([$$193; $$140; $$191]) {
                                  aggregate [$$181] <- [agg-sum($$201)]
                                  -- AGGREGATE  |LOCAL|
                                    nested tuple source
                                    -- NESTED_TUPLE_SOURCE  |LOCAL|
                               }
                        -- SORT_GROUP_BY[$$202]  |PARTITIONED|
                          exchange
                          -- HASH_PARTITION_EXCHANGE [$$202]  |PARTITIONED|
                            group by ([$$202 := $$196]) decor ([$$193; $$140; $$191]) {
                                      aggregate [$$201] <- [agg-count({"o": $$o})]
                                      -- AGGREGATE  |LOCAL|
                                        select (not(is-missing($$197)))
                                        -- STREAM_SELECT  |LOCAL|
                                          project ([$$o, $$197])
                                          -- STREAM_PROJECT  |LOCAL|
                                            nested tuple source
                                            -- NESTED_TUPLE_SOURCE  |LOCAL|
                                   }
                            -- PRE_CLUSTERED_GROUP_BY[$$196]  |PARTITIONED|
                              exchange
                              -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                                order (ASC, $$196)
                                -- STABLE_SORT [$$196(ASC)]  |PARTITIONED|
                                  exchange
                                  -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                                    project ([$$140, $$191, $$193, $$o, $$197, $$196])
                                    -- STREAM_PROJECT  |PARTITIONED|
                                      exchange
                                      -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                                        left outer join (eq($$189, $$192))
                                        -- HYBRID_HASH_JOIN [$$192][$$189]  |PARTITIONED|
                                          exchange
                                          -- HASH_PARTITION_EXCHANGE [$$192]  |PARTITIONED|
                                            running-aggregate [$$196] <- [create-query-uid()]
                                            -- RUNNING_AGGREGATE  |PARTITIONED|
                                              project ([$$140, $$191, $$193, $$192])
                                              -- STREAM_PROJECT  |PARTITIONED|
                                                exchange
                                                -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                                                  join (gt($$191, $$188))
                                                  -- NESTED_LOOP  |PARTITIONED|
                                                    exchange
                                                    -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                                                      assign [$$140] <- [{"c_acctbal": $$191, "c_custkey": $$192, "cntrycode": substring($$193, 0, 2)}]
                                                      -- ASSIGN  |PARTITIONED|
                                                        assign [$$193, $$192, $$191] <- [$$Customer.getField(4), $$Customer.getField(0), $$Customer.getField(5)] project: [$$193, $$192, $$191]
                                                        -- ASSIGN  |PARTITIONED|
                                                          exchange
                                                          -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                                                            data-scan []<-[$$Customer] <- test.Customer
                                                            -- DATASOURCE_SCAN  |PARTITIONED|
                                                              exchange
                                                              -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                                                                empty-tuple-source
                                                                -- EMPTY_TUPLE_SOURCE  |PARTITIONED|
                                                    exchange
                                                    -- BROADCAST_EXCHANGE  |PARTITIONED|
                                                      assign [$$188] <- [get-item($$154, 0)] project: [$$188]
                                                      -- ASSIGN  |UNPARTITIONED|
                                                        aggregate [$$154] <- [listify($$200)]
                                                        -- AGGREGATE  |UNPARTITIONED|
                                                          aggregate [$$200] <- [agg-global-sql-avg($$206)]
                                                          -- AGGREGATE  |UNPARTITIONED|
                                                            exchange
                                                            -- RANDOM_MERGE_EXCHANGE  |PARTITIONED|
                                                              aggregate [$$206] <- [agg-local-sql-avg($$194)]
                                                              -- AGGREGATE  |PARTITIONED|
                                                                select (gt($$194, 0.0))
                                                                -- STREAM_SELECT  |PARTITIONED|
                                                                  assign [$$194] <- [$$186.getField(5)] project: [$$194]
                                                                  -- ASSIGN  |PARTITIONED|
                                                                    exchange
                                                                    -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                                                                      data-scan []<-[$$186] <- test.Customer
                                                                      -- DATASOURCE_SCAN  |PARTITIONED|
                                                                        exchange
                                                                        -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                                                                          empty-tuple-source
                                                                          -- EMPTY_TUPLE_SOURCE  |PARTITIONED|
                                          exchange
                                          -- HASH_PARTITION_EXCHANGE [$$189]  |PARTITIONED|
                                            assign [$$197, $$189] <- [true, $$o.getField(1)]
                                            -- ASSIGN  |PARTITIONED|
                                              exchange
                                              -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                                                data-scan []<-[$$o] <- test.Orders
                                                -- DATASOURCE_SCAN  |PARTITIONED|
                                                  exchange
                                                  -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                                                    empty-tuple-source
                                                    -- EMPTY_TUPLE_SOURCE  |PARTITIONED|
