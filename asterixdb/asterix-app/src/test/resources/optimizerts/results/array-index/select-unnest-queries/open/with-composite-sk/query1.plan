distribute result [$$38]
-- DISTRIBUTE_RESULT  |PARTITIONED|
  exchange
  -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
    assign [$$38] <- [{"business_id": $$39}] project: [$$38]
    -- ASSIGN  |PARTITIONED|
      select (and(eq("19:49:16", $$D.getField("time")), eq("2016-04-26", $$D.getField("date")))) project: [$$39]
      -- STREAM_SELECT  |PARTITIONED|
        unnest $$D <- scan-collection($$41) project: [$$39, $$D]
        -- UNNEST  |PARTITIONED|
          select (eq("--1UhMGODdWsrMastO9DZw", $$39))
          -- STREAM_SELECT  |PARTITIONED|
            assign [$$39, $$41] <- [$$C.getField("business_id"), $$C.getField("checkin_times")] project: [$$39, $$41]
            -- ASSIGN  |PARTITIONED|
              project ([$$C])
              -- STREAM_PROJECT  |PARTITIONED|
                exchange
                -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                  unnest-map [$$40, $$C] <- index-search("YelpCheckin", 0, "Default", "TestYelp", "YelpCheckin", false, false, 1, $$50, 1, $$50, true, true, true)
                  -- BTREE_SEARCH  |PARTITIONED|
                    exchange
                    -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                      distinct ([$$50])
                      -- PRE_SORTED_DISTINCT_BY  |PARTITIONED|
                        exchange
                        -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                          order (ASC, $$50)
                          -- STABLE_SORT [$$50(ASC)]  |PARTITIONED|
                            exchange
                            -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                              project ([$$50])
                              -- STREAM_PROJECT  |PARTITIONED|
                                exchange
                                -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                                  unnest-map [$$48, $$49, $$50] <- index-search("IdxYelpCheckinDatesTimes", 0, "Default", "TestYelp", "YelpCheckin", false, false, 2, $$44, $$45, 2, $$46, $$47, true, true, true)
                                  -- BTREE_SEARCH  |PARTITIONED|
                                    exchange
                                    -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                                      assign [$$44, $$45, $$46, $$47] <- ["2016-04-26", "19:49:16", "2016-04-26", "19:49:16"]
                                      -- ASSIGN  |PARTITIONED|
                                        empty-tuple-source
                                        -- EMPTY_TUPLE_SOURCE  |PARTITIONED|
