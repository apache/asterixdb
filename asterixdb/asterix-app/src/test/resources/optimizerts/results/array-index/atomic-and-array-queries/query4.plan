distribute result [$$41]
-- DISTRIBUTE_RESULT  |PARTITIONED|
  exchange
  -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
    assign [$$41] <- [{"D": $$D}] project: [$$41]
    -- ASSIGN  |PARTITIONED|
      select ($$36) project: [$$D]
      -- STREAM_SELECT  |PARTITIONED|
        project ([$$D, $$36])
        -- STREAM_PROJECT  |PARTITIONED|
          subplan {
                    aggregate [$$36] <- [non-empty-stream()]
                    -- AGGREGATE  |LOCAL|
                      select (and(eq($$47, 2), eq($$46, 3)))
                      -- STREAM_SELECT  |LOCAL|
                        assign [$$47, $$46] <- [$$DI.getField("field2"), $$DI.getField("field3")] project: [$$47, $$46]
                        -- ASSIGN  |LOCAL|
                          unnest $$DI <- scan-collection($$45) project: [$$DI]
                          -- UNNEST  |LOCAL|
                            project ([$$45])
                            -- STREAM_PROJECT  |LOCAL|
                              nested tuple source
                              -- NESTED_TUPLE_SOURCE  |LOCAL|
                 }
          -- SUBPLAN  |PARTITIONED|
            select (and(eq($$D.getField("field4"), 4), eq($$D.getField("field1"), 1)))
            -- STREAM_SELECT  |PARTITIONED|
              assign [$$45] <- [$$D.getField("items")]
              -- ASSIGN  |PARTITIONED|
                project ([$$D])
                -- STREAM_PROJECT  |PARTITIONED|
                  exchange
                  -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                    unnest-map [$$42, $$D] <- index-search("Dataset1", 0, "Default", "TestDataverse", "Dataset1", false, false, 1, $$60, 1, $$60, true, true, true)
                    -- BTREE_SEARCH  |PARTITIONED|
                      exchange
                      -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                        distinct ([$$60])
                        -- PRE_SORTED_DISTINCT_BY  |PARTITIONED|
                          exchange
                          -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                            order (ASC, $$60)
                            -- STABLE_SORT [$$60(ASC)]  |PARTITIONED|
                              exchange
                              -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                                project ([$$60])
                                -- STREAM_PROJECT  |PARTITIONED|
                                  exchange
                                  -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                                    unnest-map [$$56, $$57, $$58, $$59, $$60] <- index-search("d1Idx", 0, "Default", "TestDataverse", "Dataset1", false, false, 4, $$48, $$49, $$50, $$51, 4, $$52, $$53, $$54, $$55, true, true, true)
                                    -- BTREE_SEARCH  |PARTITIONED|
                                      exchange
                                      -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                                        assign [$$48, $$49, $$50, $$51, $$52, $$53, $$54, $$55] <- [1, 2, 3, 4, 1, 2, 3, 4]
                                        -- ASSIGN  |PARTITIONED|
                                          empty-tuple-source
                                          -- EMPTY_TUPLE_SOURCE  |PARTITIONED|
