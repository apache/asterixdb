distribute result [$$73]
-- DISTRIBUTE_RESULT  |UNPARTITIONED|
  exchange
  -- ONE_TO_ONE_EXCHANGE  |UNPARTITIONED|
    aggregate [$$73] <- [agg-sql-sum($$80)]
    -- AGGREGATE  |UNPARTITIONED|
      exchange
      -- RANDOM_MERGE_EXCHANGE  |PARTITIONED|
        aggregate [$$80] <- [agg-sql-count(1)]
        -- AGGREGATE  |PARTITIONED|
          exchange
          -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
            distinct ([$$87])
            -- PRE_SORTED_DISTINCT_BY  |PARTITIONED|
              exchange
              -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                order (ASC, $$87)
                -- STABLE_SORT [$$87(ASC)]  |PARTITIONED|
                  exchange
                  -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                    select (and(ge($$69, 0), lt($$69, 1000000), lt($$74, date: { 2002-11-09 }), ge($$74, date: { 1997-05-19 }))) project: [$$87]
                    -- STREAM_SELECT  |PARTITIONED|
                      exchange
                      -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                        unnest-map [$$74, $$69, $$87] <- index-search("create_at_status_count_idx", 0, "Default", "twitter", "ds_tweet", false, false, 2, $$81, $$82, 2, $$83, $$84, true, true, false)
                        -- BTREE_SEARCH  |PARTITIONED|
                          exchange
                          -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                            assign [$$81, $$82, $$83, $$84] <- [date: { 1997-05-19 }, 0, date: { 2002-11-09 }, 1000000]
                            -- ASSIGN  |PARTITIONED|
                              empty-tuple-source
                              -- EMPTY_TUPLE_SOURCE  |PARTITIONED|
