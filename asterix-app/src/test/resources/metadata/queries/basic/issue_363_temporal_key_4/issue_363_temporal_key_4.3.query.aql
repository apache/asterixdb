/*
 * Description  : create a dataset providing hints but use  whitespace 
 * Expected Res : Success
 * Date         : 29 Jan 2013
 * Issue        : 251
 */
 
use dataverse test;

for $x in dataset('Employee')
where $x.id > day-time-duration("P10D")
return $x
