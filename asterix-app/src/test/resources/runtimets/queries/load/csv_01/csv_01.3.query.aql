/**
 *
 * CSV file loading test
 * Expected result: success
 *
 */

use dataverse temp;

for $i in dataset testds
return { "id": $i.id, 
         "float": $i.float, 
         "double": $i.double, 
         "date": date($i.date), 
         "time": time($i.time),
         "datetime": datetime($i.datetime)
         }